{
  "name": "sample-cypress",
  "version": "1.0.0",
  "description": "Zebrunner Cypress reporting agent sample",
  "scripts": {
    "test": "npx cypress run --headed --spec cypress/e2e/demo.cy.js"
  },
  "devDependencies": {
    "cypress": "^12.9.0"
  },
  "dependencies": {
    "@zebrunner/javascript-agent-cypress": "^1.1.0",
    "cypress-xpath": "^2.0.1"
  }
}
